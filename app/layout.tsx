import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 5,
  themeColor: "#000000",
};

export const metadata: Metadata = {
  title: "Voltis Emoto — Premium Electric Motorcycles",
  description: "Premium electric motorcycles engineered for performance. Explore the Voltis Emoto range — Australian-backed, rider-tested.",
  openGraph: {
    siteName: "Voltis Emoto",
    title: "Voltis Emoto — Premium Electric Motorcycles",
    description: "Premium electric motorcycles engineered for performance. Explore the Voltis Emoto range.",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
